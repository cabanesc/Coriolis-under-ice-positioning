%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
% CONFIGURATION - START

% Multiple MATLAB paths separated by semicolon
PATHS_NAME = /home/dir1/;/home/dir2


% default list of floats to process
FLOAT_LIST_FILE_NAME = ./list_arvor_arcticgo.txt
% FLOAT_LIST_FILE_NAME = C:\Users\jprannou\_RNU\DecArgo_soft\lists\liste_snapshot_202202.txt
% FLOAT_LIST_FILE_NAME = C:\Users\jprannou\_RNU\DecArgo_soft\lists\liste_snapshot_202202_prof_qc_8_9.txt

% top directory of the NetCDF files
DIR_INPUT_NC_FILES = /home/nicolas/Work/Data/ARGO/ARVOR-ICE-Pour-Camille/
% DIR_INPUT_NC_FILES = D:\202202-ArgoData\coriolis\

% directory of output files
DIR_OUTPUT_FILES = ./

% directory to store the log file
DIR_LOG_FILE = ./log/

% GEBCO bathymetric file
GEBCO_FILE = ./GEBCO_2022.nc

% [cc 01/2026 ---->
% Choice of initial path used to initialize the terrain-following method
% When INIT_PATH_MODE = GEODESIC, the initial path is the shortest path on the Earth's surface (geodesic), between the first and last known positions.
% When INIT_PATH_MODE = BATHY,  the initial path follows a smoothed bathymetric contour (isobath-based path) between the first and last known positions.
INIT_PATH_MODE = GEODESIC
% ...cc 01/2026]

% max difference (in meters) between sea bottom and float parking drift to start
DIFF_DEPTH_TO_START = 100000

% tolerance (in meters) used to compare float pressure and GEBCO depth
FLOAT_VS_BATHY_TOLERANCE = 10

% tolerance (in meters) used to compare float pressure and GEBCO depth when the
% float grounded
FLOAT_VS_BATHY_TOLERANCE_FOR_GRD = 170

% first range value (in kilometers)
FIRST_RANGE = 20

% last range value (in kilometers)
LAST_RANGE = 250

% range period (in kilometers)
RANGE_PERIOD = 10

% [cc 01/2026 ---->
% The terrain_following algorithm will not search for a  path farther than
% MAX_EXTEND × RANGE (in kilometers) away from the initial path 
% (applies only if INIT_PATH_MODE = BATHY)
% This parameter limits excessive deviations from the initial trajectory 
% and helps prevent unrealistic paths.
% leave it empty for no restriction

MAX_EXTEND = 10

% Azimuth mode for defining search locations:
% local    : normal to the instantaneous direction of the initial path
% mean     : normal to a mean direction obtained by parallel transport from
%           the geodesic connecting the first and last path points (default)
% Both options are equivalent when the initial path is the shortest path on the Earth's surface (geodesic)

AZIMUTH_MODE = mean

% Absolute latitude threshold (degrees) above which a stereographic projection is used for map plotting purposes only.

LAT_THRESHOLD = 60

% ...cc 01/2026]

% plot types
PLOT_PNG = 1
PLOT_PDF = 1


% CONFIGURATION - END
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%